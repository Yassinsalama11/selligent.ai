[build]
builder = "RAILPACK"

[deploy]
startCommand = "node airos/backend/src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
