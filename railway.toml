[build]
builder = "RAILPACK"

[deploy]
releaseCommand = "cd packages/db && npx prisma migrate deploy"
startCommand = "node airos/backend/src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
