apiVersion: 1

providers:
  - name: ChatOrAI Observability
    folder: ChatOrAI
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
