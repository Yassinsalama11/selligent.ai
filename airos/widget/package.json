{
  "name": "chatorai-widget",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/widget.js --bundle --minify --outfile=dist/widget.js"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
